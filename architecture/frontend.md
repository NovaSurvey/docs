Updating the Influential Units module to the 3-column "Command Center" architecture is absolutely the right move. That empty dark space on the right is a missed opportunity for powerful data visualization.Influential units are statistically distinct from standard outliers. A standard outlier (Editing module) is just a weird, extreme number. An influential unit is a number that might look normal, but has a massive survey weight attached to it, meaning it disproportionately warps the final population estimate.Because of this, your visualizations and AI messages need to focus on weighted impact, not just distribution.Here is the exact blueprint for your middle and right panels.The Middle Panel: Live Data Insights (The Visuals)This panel must visually prove why a unit is influential and exactly how your treatment (like Winsorization) is fixing it.1. Pre-Run (The Diagnostic State): The Impact Scatter PlotThe Setup: Render a high-performance ECharts scatter plot.X-Axis: The raw value (Target Variable, e.g., revenue).Y-Axis: The survey weight (Initial Weight Variable).The "Danger Zone" Curve: Because impact is roughly $Weight \times Value$, draw a curved threshold line (an isoline) across the plot based on the user's Threshold Value input.The Result: Most data lives in the bottom-left. The influential units are the dots sitting in the top-right quadrant, visibly breaking past the threshold curve. It makes the math instantly intuitive.2. Post-Run (The Impact State): The Winsorization Vector PlotThe Setup: Once the module runs, update the scatter plot to show the treatment.The Visual: Leave the original points as faded gray dots. Draw the new (winsorized/adjusted) points as bright blue dots, and connect them with a small arrow or line.The KPI Dashboard: Below the chart, you must show the bottom-line business impact. Display two massive numbers: Original Population Total vs. Adjusted Population Total. Statisticians need to know immediately if modifying these 5 units dropped the national revenue estimate by $200 million.The Third Panel: Smart AI Data AnalystFollowing the paradigm we established, the AI should act as a Copilot, shifting from Strategy to Audit.1. Strategy Mode (Pre-Run)The Threshold Optimizer: Choosing a tuning constant (like the 15 in your screenshot) is often a guessing game for users. Your Polars backend can quickly test a few constants in the background. The AI should read that and advise.Example Output: 💡 Suggestion: "A tuning constant of 15 will flag 42 units (0.8% of the dataset). Consider increasing to 20 to restrict treatment strictly to the top 0.1% most volatile units."Strata Vulnerability Warning: The AI should identify which specific partition has the highest concentration of heavy weights.Example Output: ⚠️ Insight: "The Construction strata contains 3 units with weights exceeding 5,000. This specific domain is highly sensitive to influential values."2. Audit Mode (Post-Run)The Impact Summary: A plain-English explanation of the statistical damage control.Example Output: 📊 Audit Complete: "Winsorization successfully reduced the impact of 14 units. This lowered the global estimated total by 2.4%, significantly stabilizing the domain variance without destroying the underlying distribution."The "Over-Treatment" Alert: If the user's threshold was too aggressive and changed too much data, the AI acts as a guardrail.Example Output: 🛑 Warning: "Treatment altered 8% of the total estimate. Industry standard recommends influential unit adjustments should not exceed a 3-5% impact on the final estimate. Consider relaxing the threshold."To make sure we get the X and Y axes on that scatter plot exactly right, are you using a simple Weight-Share method (Weight × Value) to calculate total impact under the hood, or are you using a more complex Conditional Bias metric?